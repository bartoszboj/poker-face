/* eslint-disable @typescript-eslint/no-duplicate-enum-values */

enum EnumLogin {
    SIGN_IN_KEY = 'sign-in',
    SIGN_UP_KEY = 'register',
    USERNAME_MAX_CHARS = 20,
    NAME_MAX_CHARS = 50,
    PASSWORD_MAX_CHARS = 50,
}

export default EnumLogin
